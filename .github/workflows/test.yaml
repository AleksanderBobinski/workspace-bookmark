name: test
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    container: ghcr.io/aleksanderbobinski/workspace-bookmark:develop
    steps:
      - uses: actions/checkout@v3
        with:
          path: ./workspace-bookmark
      - run: 'rm -Rf test && ./workspace-bookmark/create_test_dir.sh'
      - run: bash -c 'source ./workspace-bookmark/setup.sh && cd test && python3 -m pytest ../workspace-bookmark/test_workspace_bookmark.py'
      - run: bash -c 'source ./workspace-bookmark/setup.sh && cd test && ../workspace-bookmark/test_g.bats'

